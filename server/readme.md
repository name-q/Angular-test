# mock-server

## 目录结构

```
mock-server/
├── server.js           # 入口文件
├── routes/             # 存放所有接口的文件夹
│   ├── getRoute.js     # GET 请求接口
│   └── postRoute.js    # POST 请求接口
└── utils/              # 其他工具文件夹（如 mock 数据）
    └── mockData.js     # 模拟数据文件
```